# A `data.frame` containing validation rules and names

A `data.frame` containing validation rules and names

## Usage

``` r
rule_map
```

## Format

`data.frame` containing 23 rows and 3 variables

- pat:

  validation pattern

- col:

  column name that is being validated

- issue:

  what is the issue that the column has
