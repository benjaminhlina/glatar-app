# Ram Tracker

Tracks the amoutn of RAM used during shiny app deployment.

## Usage

``` r
ram_tracker()
```

## Value

displays the amont of RAM used during app deployment. Checks RAM usage
every 5 seeconds and returns values in mb.
