# Documentation Tab

Provides the documentation tab.

## Usage

``` r
docs_ui(id)

docs_server(id)
```

## Arguments

- id:

  the shiny namespace id name (i.e., `"docs"`).

## Details

`docs_ui()` a shiny UI.

`docs_server()` a shiny module server.
