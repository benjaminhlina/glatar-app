# A `data.frame` containing naming conventions

A `data.frame` containing naming conventions

## Usage

``` r
naming_conventions
```

## Format

`data.frame` containing 87 rows and 2 variables

- raw_names:

  name database columns

- nice_names:

  nice names to transform them into
