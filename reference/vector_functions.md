# Vector functions

These functions produce a vector with information related to the
database. These may eventually be moved to being data objects that are
loaded when the package is loaded.

## Usage

``` r
data_tables()

data_types()

good_groups()

protected_tabs()

themes()
```
