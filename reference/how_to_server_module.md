# How to Use Server

Provides the server for the documentation tab

## Usage

``` r
how_to_server(id, parent_session)
```

## Arguments

- id:

  the shiny namespace id name (i.e., `"how_to_use"`).

- parent_session:

  a shiny server session.

## Value

`how_to_server()` a shiny module server.
