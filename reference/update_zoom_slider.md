# Update Zoom Slider

This updates the zoom sliders

## Usage

``` r
update_zoom_slider(id, vec)
```

## Arguments

- id:

  the shiny id used e.g., `zoom_x`.

- vec:

  a `vector` containing the minamum and maximum values.

## Value

an updated Input object for a given `zoom_*` object.
