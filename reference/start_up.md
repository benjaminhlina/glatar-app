# Start up GLATAR App

This function, which has no arguments, starts the database connection,
gets ``` valid_values`` for validation, gets  ```nice_names\` for
properly displaying tables, creates credientials, and and file location
for resources.

## Usage

``` r
start_up()
```
