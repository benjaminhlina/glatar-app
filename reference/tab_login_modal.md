# Tab Login Modal

This function produces the login modal when clicking on the protected
tabs `raw_data` and `upload_data`.

## Usage

``` r
tab_login_modal(failed = FALSE)
```

## Arguments

- failed:

  a logical value that when supplied produces a warning message stating
  invalid email or password. Default is `FALSE`
