# This function produces the UI for the registraiton fields both in the modal and tab

This function produces the UI for the registraiton fields both in the
modal and tab

## Usage

``` r
register_fields(...)
```

## Arguments

- ...:

  additional parameters to be given for example a `NS()` object.

## Value

shiny UI objects
